# 易记移动端设计基线

## 设计意图

“易记”是一款像翻书一样自然的移动刷题工具。界面深色、克制、安静，优先保证题干阅读、单手操作和反馈辨识；视觉身份不绑定电力行业，以便未来装载其他考试内容包。不使用紫色渐变、装饰性图形、任务打卡氛围或泛 SaaS 卡片堆叠。

## 体验隐喻

- 题库首页是目录：直接选择知识域或题型。
- 继续上次是书签：存在断点时置顶，不制造每日任务。
- 收藏与统计页是摘抄本：保留重点和历史，不用连续天数施压。
- 错题本是动态夹页：答错加入，之后答对自动移出。

## 颜色

- `--bg: #0B1117`：页面背景
- `--surface: #121B23`：主容器
- `--surface-raised: #18242D`：可操作区域
- `--line: #2A3944`：边界与分隔
- `--text: #F2F6F3`：主文字
- `--muted: #91A3AE`：辅助文字
- `--accent: #F4B74A`：主行动与书签提示
- `--correct: #53D3B1`：正确与完成
- `--danger: #FF6B6B`：错误与警示
- `--info: #69A9FF`：中性信息

## 字体

- 中文正文：`Noto Sans SC`, 400 / 500 / 600 / 700
- 数字、题号、计时：`IBM Plex Mono`, 500 / 600
- 页面正文：最小 17px / 1.65
- 题干：默认 22px / 1.75，600
- 选项：最小 16px / 1.65
- 一级标题：20px / 1.35，700
- 主按钮：17px / 1.2，700
- 辅助文字：最小 13px / 1.55
- 底部导航：12px / 1.2，500
- 系统字体放大时允许纵向滚动，不缩小字号以维持单屏布局。

## 间距与形状

- 间距序列：4、8、12、16、20、24、32px
- 小圆角：10px；容器圆角：16px；手机外壳：30px
- 主要按钮高度：56px；选项最小高度：64px；普通触控区域最小：44px
- 相邻操作最小间距：8px
- 默认用 1px 边框形成层级，避免依赖大面积阴影。

## 组件规则

- 每屏只保留一个琥珀色主行动。
- 选项为整行触控区域；选择前为边框态，选择后为强调色描边与序号实心态。
- 正确、错选、漏选同时依靠图标、文字和颜色区分，不能仅依靠颜色。
- 判题反馈不出现四档自评；底部固定“下一题”。
- 错题状态使用一句结果说明：“已加入错题本”或“已从错题本移出”。
- 底部导航固定，三个入口顺序为：题库、模考、统计。
- 错题本属于“统计”的二级页面；统计首页提供醒目的错题入口，错题页保留返回统计的明确路径。
- 沉浸答题时隐藏一级导航，改为顶部退出、题号和收藏，底部固定提交区。
- 模考只允许选择已冻结的试卷，不显示题量、时长或题型比例设置器。

## 题库首页层级

1. 当前内容包名称与设置入口；
2. 有断点时显示“继续上次”大卡片；
3. 常用知识域列表与“查看全部”；
4. 单选、多选、判断三个题型入口。

不展示“今日”、待复习队列、完成环、连续学习或目标进度。

## 单手操作

- 高频动作放在屏幕中下部；主操作固定在底部安全区。
- 选项与列表整行可点，避免只点小图标。
- 返回/退出位于左上但自动保存，不要求用户再点确认。
- 提前交卷等不可逆动作必须二次确认。

## 响应式

- ≤375px：原生手机页面，左右边距 16px，允许内容自然纵向滚动。
- 376–767px：手机或窄平板，内容最大宽度 480px。
- 768–1023px：展示一台手机预览与状态选择器。
- ≥1024px：原型评审台，左侧说明、中央手机、右侧交互注释。

## 动效

- 页面状态切换 160–220ms，使用透明度和 6px 内的位移。
- 判题反馈允许一次轻微强调，不使用循环动画。
- 遵守 `prefers-reduced-motion`，关闭非必要过渡。
